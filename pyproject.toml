[project]
name = "iscatbot"
version = "1.1.1"
description = "投稿された画像に猫が映っているかどうかを判定するDiscord botです。"
readme = ".github/README.md"
requires-python = ">=3.13"
dependencies = [
    "discord-py>=2.7.1",
    "dotenv>=0.9.9",
    "google-genai>=2.12.1",
]
